{
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install --filter=loop.hackathon --no-frozen-lockfile",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ':!Backend/**'"
}